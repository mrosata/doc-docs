# Main entry point for the Doc Docs application to run.
from doc_docs import app

if __name__ == '__main__':
    # app.run(host='0.0.0.0', port=5000, debug=True)
    # app.run(port=5000, host='0.0.0.0')
    app.run()
